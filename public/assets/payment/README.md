# Payment QR

Drop the official Google Pay payment QR image here as:

```
gpay-qr.png
```

The registration payment screen loads `/assets/payment/gpay-qr.png`.

Until the real QR exists, the UI shows a clearly-marked placeholder box.
Keep the file sharp and high-contrast so it stays scannable.